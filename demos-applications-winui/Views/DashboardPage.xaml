<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="demos_applications_winui.Views.DashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:models="using:demos_applications_winui.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="DashboardItemTemplate" x:DataType="models:DashboardItem">
            <Button Command="{Binding NavigateToItemCommand}"
                    CommandParameter="{x:Bind}"
                    HorizontalAlignment="Stretch"
                    HorizontalContentAlignment="Stretch"
                    Padding="0"
                    BorderThickness="0"
                    Background="Transparent">
                <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                      BorderThickness="1"
                      BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                      CornerRadius="{StaticResource OverlayCornerRadius}"
                      Padding="20">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <FontIcon Glyph="{x:Bind Glyph}"
                              FontSize="32"
                              Foreground="{ThemeResource AccentButtonBackground}"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,12"/>

                    <TextBlock Grid.Row="1"
                               Text="{x:Bind Title}"
                               Style="{StaticResource SubtitleTextBlockStyle}"
                               Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="2"
                               Text="{x:Bind Description}"
                               Style="{StaticResource BodyTextBlockStyle}"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                               TextWrapping="Wrap"/>
                </Grid>
            </Button>
        </DataTemplate>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Dashboard" Margin="16,16,16,8"
                   Style="{StaticResource TitleTextBlockStyle}"/>

        <ScrollViewer Grid.Row="1" Padding="16">
            <ItemsRepeater ItemsSource="{x:Bind ViewModel.Items}"
                           ItemTemplate="{StaticResource DashboardItemTemplate}">
                <ItemsRepeater.Layout>
                    <UniformGridLayout MinItemWidth="240"
                                       MinColumnSpacing="16"
                                       MinRowSpacing="16"
                                       ItemsJustification="Start"/>
                </ItemsRepeater.Layout>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
