<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="demos_applications_winui.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:demos_applications_winui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="demos-applications-winui">

    <Window.SystemBackdrop>
        <MicaBackdrop Kind="Base"/>
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        
        <Grid.RowDefinitions>
            <!-- Title Bar -->
            <RowDefinition Height="Auto" />
            <!-- App Content -->
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <TitleBar x:Name="AppTitleBar"
          Title="WinUI Notes"
          IsBackButtonVisible="{x:Bind NavigationState.CanGoBack, Mode=OneWay}"
          BackRequested="AppTitleBar_BackRequested">

            <TitleBar.Content>
                <StackPanel Orientation="Horizontal" Spacing="8"
                            HorizontalAlignment="Right"
                            Visibility="{x:Bind AuthState.IsAuthenticated, Mode=OneWay}">
                    <TextBlock Text="{x:Bind AuthState.Username, Mode=OneWay}"
                               VerticalAlignment="Center"
                               Style="{StaticResource CaptionTextBlockStyle}"/>
                    <Button Content="Sign out"
                            Click="LogoutButton_Click"
                            Padding="8,4"/>
                </StackPanel>
            </TitleBar.Content>

        </TitleBar>

        <!-- App content -->
        <Frame x:Name="rootFrame" Grid.Row="1">
            <Frame.ContentTransitions>
                <TransitionCollection>
                    <ContentThemeTransition/>
                </TransitionCollection>
            </Frame.ContentTransitions>
        </Frame>

        <!-- Toast overlay (DI-resolved, added in code-behind) -->

    </Grid>
    
</Window>
