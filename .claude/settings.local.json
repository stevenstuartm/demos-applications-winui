{
  "permissions": {
    "allow": [
      "Bash(git mv demos-applications-winui/Modals demos-applications-winui/Models)",
      "Bash(dotnet restore)",
      "Bash(dotnet build -p:Platform=x64)"
    ]
  }
}
