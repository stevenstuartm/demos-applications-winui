<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="demos_applications_winui.Toolkit.Toast.ToastPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:demos_applications_winui.Toolkit.Toast"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Right" VerticalAlignment="Bottom"
          MaxWidth="400" Margin="0,0,16,16">
        <ItemsControl ItemsSource="{x:Bind ToastState.Toasts}">
            <ItemsControl.ItemTemplate>
                <DataTemplate x:DataType="local:ToastItem">
                    <InfoBar Title="{x:Bind Title}"
                             Message="{x:Bind Message}"
                             Severity="{x:Bind local:ToastPresenter.ConvertSeverity(Severity)}"
                             IsOpen="True"
                             IsClosable="True"
                             Closed="Toast_Closed"
                             Margin="0,4,0,0"/>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>

</UserControl>
